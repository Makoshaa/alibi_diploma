# Система мониторинга посещаемости с распознаванием лиц

![Node.js](https://img.shields.io/badge/Node.js-18+-green.svg)
![React](https://img.shields.io/badge/React-18.3-blue.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-14+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

Веб-приложение для автоматизированного учета посещаемости сотрудников с использованием технологии распознавания лиц и проверки живости (liveness detection).

## 📚 Документация

- **[INSTALL.md](./INSTALL.md)** - Краткая инструкция по установке (5 минут)
- **[QUICK_START.md](./QUICK_START.md)** - Быстрый старт
- **[API.md](./API.md)** - Полная документация API
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** - Архитектура приложения
- **[DEPLOYMENT.md](./DEPLOYMENT.md)** - Руководство по развертыванию
- **[NETWORK_SETUP.md](./NETWORK_SETUP.md)** - Настройка локальной сети
- **[FAQ.md](./FAQ.md)** - Часто задаваемые вопросы
- **[CHANGELOG.md](./CHANGELOG.md)** - История изменений

## 📋 Содержание

- [Возможности](#возможности)
- [Технологический стек](#технологический-стек)
- [Архитектура](#архитектура)
- [Предварительные требования](#предварительные-требования)
- [Установка](#установка)
- [Запуск](#запуск)
- [Работа в локальной сети](#работа-в-локальной-сети)
- [Структура проекта](#структура-проекта)
- [API Endpoints](#api-endpoints)
- [База данных](#база-данных)
- [Безопасность](#безопасность)
- [Разработка](#разработка)
- [Production](#production)

## 🎯 Возможности

### Для сотрудников:
- ✅ Регистрация лица через веб-камеру или мобильное устройство
- ✅ Отметка прихода/ухода с работы по распознаванию лица
- ✅ Проверка живости (защита от фотографий и видео)
- ✅ Просмотр истории посещений
- ✅ QR-код для быстрого доступа с мобильного устройства

### Для администраторов:
- ✅ Управление пользователями (добавление, редактирование, удаление)
- ✅ Просмотр и управление записями о посещаемости
- ✅ Статистика по сотрудникам
- ✅ Экспорт данных

## 🛠 Технологический стек

### Frontend
- **React 18.3** - библиотека для построения пользовательского интерфейса
- **React Router 6.23** - маршрутизация
- **Vite 5.2** - сборщик и dev-сервер
- **TensorFlow.js 4.21** - машинное обучение в браузере
- **@vladmandic/face-api 1.7** - распознавание лиц
- **QRCode.react 4.2** - генерация QR-кодов
- **Axios 1.7** - HTTP-клиент

### Backend
- **Node.js + Express 4.19** - серверный фреймворк
- **Prisma 5.15** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **JWT (jsonwebtoken 9.0)** - аутентификация
- **bcryptjs 2.4** - хеширование паролей
- **Multer 2.0** - загрузка файлов
- **CORS 2.8** - управление cross-origin запросами

### Безопасность
- **HTTPS** с самоподписанными сертификатами
- **JWT токены** с HttpOnly cookies
- **bcrypt** для хеширования паролей
- **Валидация данных** с помощью Zod

## 🏗 Архитектура

```
┌─────────────────────────────────────────────────────┐
│                   Client (React)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │
│  │  Landing     │  │   Admin      │  │ Employee  │ │
│  │  Page        │  │   Panel      │  │ Dashboard │ │
│  └──────────────┘  └──────────────┘  └───────────┘ │
│         │                  │                │       │
│         └──────────────────┴────────────────┘       │
│                      │                              │
│              ┌───────▼────────┐                     │
│              │  Face API      │                     │
│              │  TensorFlow.js │                     │
│              └────────────────┘                     │
└─────────────────────┬───────────────────────────────┘
                      │ HTTPS (5173)
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│              Express Server (5000)                  │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │
│  │   Auth       │  │    Admin     │  │Attendance │ │
│  │   Routes     │  │   Routes     │  │  Routes   │ │
│  └──────┬───────┘  └──────┬───────┘  └─────┬─────┘ │
│         │                 │                 │       │
│  ┌──────▼─────────────────▼─────────────────▼────┐  │
│  │            Middleware Layer                   │  │
│  │  (Auth, CORS, Cookie Parser, Error Handler)  │  │
│  └──────────────────────┬────────────────────────┘  │
│                         │                           │
│                ┌────────▼─────────┐                 │
│                │  Prisma Client   │                 │
│                └────────┬─────────┘                 │
└─────────────────────────┼───────────────────────────┘
                          │
                          ▼
                  ┌───────────────┐
                  │  PostgreSQL   │
                  │   Database    │
                  └───────────────┘
```

## 📋 Предварительные требования

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0
- **PostgreSQL** >= 14.0
- **Git** (опционально)

## 📥 Установка

### 1. Клонирование репозитория (или распаковка архива)

```bash
git clone <repository-url>
cd attendance_monitor_final
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка базы данных

Создайте PostgreSQL базу данных:

```sql
CREATE DATABASE attendance_db;
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DATABASE_URL="postgresql://postgres:password@localhost:5432/attendance_db?schema=public"

# JWT секретный ключ (замените на свой случайный ключ)
JWT_SECRET="your-super-secret-jwt-key-here"

# Разрешенные origin для CORS
CLIENT_ORIGIN="http://localhost:5173"

# Порт сервера
PORT=5000
```

### 5. Создание SSL сертификатов

SSL сертификаты уже должны быть в папке `certs/`. Если их нет, создайте их:

```bash
# Windows (PowerShell)
mkdir certs
cd certs

# Генерация приватного ключа
openssl genrsa -out localhost-key.pem 2048

# Генерация сертификата
openssl req -new -x509 -key localhost-key.pem -out localhost-cert.pem -days 365
```

### 6. Инициализация базы данных

```bash
# Генерация Prisma Client
npm run prisma:generate

# Применение миграций
npm run prisma:migrate

# Заполнение тестовыми данными (создает админа)
npm run prisma:seed
```

После seed будет создан администратор:
- **Email:** admin@mail.ru
- **Пароль:** admin123

⚠️ **Важно:** Измените пароль администратора после первого входа!

## 🚀 Запуск

### Development режим

Запустите приложение одной командой:

```bash
npm run dev
```

Эта команда:
- Автоматически определит ваш локальный IP адрес
- Создаст файл `.env.local` с правильными настройками
- Запустит сервер (порт 5000) и клиент (порт 5173) одновременно

Приложение будет доступно по адресам:
- **Локально**: https://localhost:5173
- **В сети**: https://[ваш-IP]:5173

### Первый вход

После запуска откройте https://localhost:5173 и войдите с учетными данными администратора:
- **Email:** admin@mail.ru
- **Пароль:** admin123

### Раздельный запуск

Если нужно запустить сервер и клиент отдельно:

```bash
# Запуск только сервера
npm run dev:server

# Запуск только клиента (в другом терминале)
npm run dev:client
```

### Production сборка

```bash
# Сборка клиента
npm run build

# Запуск production сервера
npm start
```

## 📱 Работа в локальной сети

Приложение автоматически настраивается для работы в локальной сети. См. подробную документацию:
- [QUICK_START.md](./QUICK_START.md) - быстрый старт
- [NETWORK_SETUP.md](./NETWORK_SETUP.md) - настройка сети

### Краткая инструкция:

1. **Запустите приложение:**
   ```bash
   npm run dev
   ```

2. **Найдите IP адрес в консоли:**
   ```
   ✅ HTTPS Server listening on port 5000
   Network: https://192.168.0.142:5000
   
   📱 Для подключения мобильных устройств:
      Откройте https://192.168.0.142:5173
   ```

3. **На мобильном устройстве:**
   - Подключитесь к той же Wi-Fi сети
   - Откройте указанный адрес в браузере
   - Примите предупреждение о сертификате

### Смена IP адреса

Если IP изменился, обновите конфигурацию:

```bash
npm run setup:network
```

## 📁 Структура проекта

```
attendance_monitor_final/
├── certs/                      # SSL сертификаты
│   ├── localhost-key.pem
│   └── localhost-cert.pem
├── datasets/                   # Данные для обучения (опционально)
├── models/                     # ML модели face-api
│   ├── face_landmark_68_model-weights_manifest.json
│   ├── face_recognition_model-weights_manifest.json
│   └── ...
├── model_lib/                  # Библиотеки моделей
├── prisma/                     # Prisma ORM
│   ├── migrations/            # Миграции БД
│   ├── schema.prisma          # Схема базы данных
│   └── seed.js                # Seed данные
├── public/                     # Статические файлы
│   └── wasm/                  # WASM модули для liveness
├── scripts/                    # Вспомогательные скрипты
│   └── generateEnv.js         # Генерация .env.local
├── server/                     # Backend
│   ├── middleware/            # Middleware (auth, error handler)
│   ├── routes/                # API routes
│   │   ├── auth.js           # Аутентификация
│   │   ├── admin.js          # Админ панель
│   │   └── attendance.js     # Посещаемость
│   ├── services/              # Бизнес-логика
│   ├── utils/                 # Утилиты
│   ├── index.js               # Точка входа сервера
│   ├── prismaClient.js        # Prisma клиент
│   └── registerTfjs.js        # Конфигурация TensorFlow
├── src/                        # Frontend
│   ├── components/            # React компоненты
│   │   ├── admin/            # Компоненты админ-панели
│   │   ├── attendance/       # Компоненты посещаемости
│   │   └── common/           # Общие компоненты
│   ├── context/              # React Context (AuthContext)
│   ├── lib/                  # Библиотеки (face-api, liveness)
│   ├── pages/                # Страницы
│   │   ├── admin/           # Админ страницы
│   │   ├── employee/        # Страницы сотрудника
│   │   ├── LandingPage.jsx
│   │   ├── LoginPage.jsx
│   │   └── MobileAttendancePage.jsx
│   ├── styles/              # CSS стили
│   ├── App.jsx              # Главный компонент
│   └── main.jsx             # Точка входа
├── .env                       # Переменные окружения (НЕ КОММИТИТЬ!)
├── .env.local                 # Локальные переменные (автогенерация)
├── package.json               # Зависимости и скрипты
├── vite.config.js            # Конфигурация Vite
├── QUICK_START.md            # Быстрый старт
├── NETWORK_SETUP.md          # Настройка сети
└── README.md                 # Этот файл
```

## 🔌 API Endpoints

### Аутентификация (`/api/auth`)

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/logout` - Выход из системы
- `GET /api/auth/me` - Получить текущего пользователя

### Посещаемость (`/api/attendance`)

- `POST /api/attendance/register-face` - Регистрация лица пользователя
- `POST /api/attendance/verify` - Проверка лица и создание записи посещаемости
- `GET /api/attendance` - Получить все записи (админ) или свои записи (сотрудник)
- `DELETE /api/attendance/:id` - Удалить запись посещаемости (только админ)

### Администрирование (`/api/admin`)

- `GET /api/admin/users` - Получить всех пользователей
- `POST /api/admin/users` - Создать нового пользователя
- `PUT /api/admin/users/:id` - Обновить пользователя
- `DELETE /api/admin/users/:id` - Удалить пользователя
- `POST /api/admin/users/:id/reset-face` - Сбросить данные лица пользователя

### Служебные

- `GET /api/health` - Проверка работоспособности сервера

## 🗄 База данных

Проект использует PostgreSQL с Prisma ORM.

### Модели данных:

#### User (Пользователь)
```prisma
- id: UUID
- email: String (уникальный)
- passwordHash: String
- role: Enum (ADMIN, EMPLOYEE)
- fullName: String
- isActive: Boolean
- createdAt: DateTime
- updatedAt: DateTime
```

#### FaceDescriptor (Дескриптор лица)
```prisma
- id: UUID
- descriptor: Float[] (128-мерный вектор)
- userId: UUID (связь с User)
- createdAt: DateTime
```

#### Attendance (Посещаемость)
```prisma
- id: UUID
- userId: UUID (связь с User)
- type: Enum (CHECK_IN, CHECK_OUT)
- confidence: Float (уверенность распознавания)
- livenessScore: Float (оценка живости)
- createdAt: DateTime
```

### Команды для работы с БД:

```bash
# Создать новую миграцию
npm run prisma:migrate

# Обновить Prisma Client
npm run prisma:generate

# Открыть Prisma Studio (GUI для БД)
npx prisma studio

# Сбросить БД (удалит все данные!)
npx prisma migrate reset
```

## 🔒 Безопасность

### Аутентификация
- JWT токены хранятся в HttpOnly cookies
- Автоматическое обновление токенов
- Защита от CSRF атак

### Пароли
- Хеширование с помощью bcrypt (10 раундов)
- Минимальная длина пароля: 6 символов

### HTTPS
- Обязательное использование HTTPS
- Самоподписанные сертификаты для development
- Let's Encrypt рекомендуется для production

### CORS
- Настроенные разрешенные origins
- Поддержка credentials
- Фильтрация по IP для локальной сети

### Распознавание лиц
- Liveness detection для защиты от спуфинга
- Минимальный порог уверенности: 0.6
- Минимальная оценка живости: 0.5

## 🛠 Разработка

### Установка новых зависимостей

```bash
# Frontend зависимость
npm install <package-name>

# Backend зависимость (если нужно разделить)
npm install <package-name>
```

### Изменение схемы БД

1. Отредактируйте `prisma/schema.prisma`
2. Создайте миграцию:
   ```bash
   npm run prisma:migrate
   ```
3. Обновите Prisma Client:
   ```bash
   npm run prisma:generate
   ```

### Отладка

#### Backend
```bash
# Запуск с NODE_ENV=development
NODE_ENV=development npm run dev:server

# Логи SQL запросов Prisma
# Добавьте в .env:
# DATABASE_URL="...?connection_limit=5&log=query"
```

#### Frontend
- Используйте React DevTools
- Проверьте консоль браузера
- Используйте Vite HMR для горячей перезагрузки

### Тестирование камеры на мобильном

1. Убедитесь, что HTTPS настроен правильно
2. Примите сертификат в браузере
3. Дайте разрешение на использование камеры
4. Проверьте console.log для отладки

## 🚢 Production

### Подготовка к деплою

1. **Обновите .env для production:**
   ```env
   DATABASE_URL="postgresql://user:password@production-db:5432/attendance"
   JWT_SECRET="strong-random-secret-key"
   CLIENT_ORIGIN="https://yourdomain.com"
   NODE_ENV="production"
   ```

2. **Получите настоящий SSL сертификат:**
   - Используйте Let's Encrypt
   - Или сертификат от вашего хостинг-провайдера

3. **Соберите приложение:**
   ```bash
   npm run build
   ```

4. **Запустите production сервер:**
   ```bash
   NODE_ENV=production npm start
   ```

### Рекомендации для production:

- ✅ Используйте процесс-менеджер (PM2, systemd)
- ✅ Настройте обратный прокси (nginx, Apache)
- ✅ Включите логирование (winston, morgan)
- ✅ Настройте мониторинг
- ✅ Регулярные бэкапы БД
- ✅ Настройте rate limiting
- ✅ Используйте CDN для статики

### Пример с PM2:

```bash
# Установка PM2
npm install -g pm2

# Запуск приложения
pm2 start server/index.js --name attendance-monitor

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

### Пример nginx конфигурации:

```nginx
server {
    listen 443 ssl;
    server_name yourdomain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # Клиентское приложение
    location / {
        root /path/to/attendance_monitor/dist;
        try_files $uri $uri/ /index.html;
    }

    # API
    location /api {
        proxy_pass https://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Модели и статика
    location /models {
        proxy_pass https://localhost:5000;
    }

    location /wasm {
        proxy_pass https://localhost:5000;
    }
}
```

## 📝 Скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev сервера и клиента одновременно |
| `npm run dev:server` | Запуск только backend сервера |
| `npm run dev:client` | Запуск только frontend клиента |
| `npm run build` | Сборка клиента для production |
| `npm start` | Запуск production сервера |
| `npm run setup:network` | Обновление IP адреса в .env.local |
| `npm run prisma:generate` | Генерация Prisma Client |
| `npm run prisma:migrate` | Применение миграций БД |
| `npm run prisma:seed` | Заполнение БД тестовыми данными |

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте, что все зависимости установлены: `npm install`
2. Проверьте, что PostgreSQL запущен и доступен
3. Проверьте правильность `.env` файла
4. Проверьте логи сервера и браузерную консоль
5. Убедитесь, что порты 5000 и 5173 свободны
6. Проверьте, что SSL сертификаты существуют в папке `certs/`

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](./LICENSE) для деталей.

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Если вы хотите помочь:

1. Fork репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Commit ваши изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Контакты

**Дипломный проект** - Система мониторинга посещаемости

Для вопросов и предложений создавайте Issues в репозитории.

## 🙏 Благодарности

- [TensorFlow.js](https://www.tensorflow.org/js) - ML в браузере
- [Face-API.js](https://github.com/justadudewhohacks/face-api.js) - Распознавание лиц
- [Prisma](https://www.prisma.io/) - Современный ORM
- [React](https://react.dev/) - UI библиотека
- [Vite](https://vitejs.dev/) - Быстрый build tool

---

**Разработано для дипломного проекта © 2025**
