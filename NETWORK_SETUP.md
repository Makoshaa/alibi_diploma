# Настройка работы в локальной сети

## Автоматическая настройка (рекомендуется)

Проект автоматически определяет IP адрес в локальной сети. Для запуска достаточно выполнить:

```bash
npm run dev
```

Скрипт автоматически:
1. Определит ваш локальный IP адрес
2. Создаст файл `.env.local` с правильными настройками
3. Запустит сервер и клиент

## Ручная настройка IP адреса

Если нужно вручную обновить IP адрес:

```bash
npm run setup:network
```

Эта команда обновит `.env.local` файл с актуальным IP адресом.

## Подключение мобильных устройств

1. **Убедитесь, что компьютер и мобильное устройство подключены к одной Wi-Fi сети**

2. **Запустите приложение:**
   ```bash
   npm run dev
   ```

3. **Найдите IP адрес в консоли:**
   При запуске сервера в консоли появится сообщение:
   ```
   ✅ HTTPS Server listening on port 5000
   Local:   https://localhost:5000
   Network: https://192.168.x.x:5000

   📱 Для подключения мобильных устройств:
      1. Убедитесь, что устройство подключено к той же Wi-Fi сети
      2. Откройте https://192.168.x.x:5173 в браузере мобильного устройства
      3. Примите предупреждение о самоподписанном сертификате
   ```

4. **На мобильном устройстве:**
   - Откройте браузер (Safari на iOS, Chrome на Android)
   - Введите адрес из консоли: `https://192.168.x.x:5173`
   - Примите предупреждение о безопасности сертификата

## Важные замечания

### Самоподписанный SSL сертификат
Приложение использует HTTPS с самоподписанным сертификатом для обеспечения безопасности.

**На мобильных устройствах:**
- **iOS Safari:** Нажмите "Продолжить" при предупреждении
- **Android Chrome:** Нажмите "Дополнительно" → "Перейти на сайт (небезопасно)"

### Требования к сети
- Компьютер и мобильное устройство должны быть в одной Wi-Fi сети
- Брандмауэр не должен блокировать порты 5000 и 5173
- Роутер должен разрешать связь между устройствами (некоторые публичные Wi-Fi сети блокируют это)

### Проверка подключения
Если возникают проблемы с подключением:

1. **Проверьте IP адрес:**
   ```bash
   # Windows
   ipconfig
   
   # macOS/Linux
   ifconfig
   ```
   Найдите IPv4 адрес вашего активного сетевого адаптера (обычно начинается с 192.168.)

2. **Проверьте доступность сервера:**
   ```bash
   # На компьютере с сервером
   curl -k https://localhost:5000/api/health
   ```

3. **Отключите брандмауэр временно** (только для тестирования)

4. **Попробуйте пересоздать .env.local:**
   ```bash
   npm run setup:network
   ```

## Структура конфигурации

### `.env` (основная конфигурация)
```env
DATABASE_URL="postgresql://..."
JWT_SECRET="..."
CLIENT_ORIGIN="http://localhost:5173"
PORT=5000
```

### `.env.local` (автоматически генерируется)
```env
VITE_API_BASE_URL=https://192.168.x.x:5000/api
VITE_APP_URL=https://192.168.x.x:5173
```

**Примечание:** `.env.local` автоматически генерируется при запуске `npm run dev`. Не редактируйте его вручную, так как он будет перезаписан.

## Смена сети

Если вы переключились на другую Wi-Fi сеть или IP адрес изменился:

1. Перезапустите приложение:
   ```bash
   npm run dev
   ```

2. Или обновите конфигурацию вручную:
   ```bash
   npm run setup:network
   ```

IP адрес будет автоматически определен заново.

## Порты

- **5000** - HTTPS API сервер
- **5173** - HTTPS клиентское приложение (Vite dev server)

Убедитесь, что эти порты свободны и не используются другими приложениями.
