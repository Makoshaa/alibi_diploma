# Руководство по внесению вклада

Спасибо за ваш интерес к проекту! Мы приветствуем любой вклад в развитие системы мониторинга посещаемости.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Commit сообщения](#commit-сообщения)
- [Pull Request процесс](#pull-request-процесс)
- [Типы вкладов](#типы-вкладов)

---

## Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать следующие правила:

### Наши обязательства

- ✅ Быть уважительным и включающим
- ✅ Приветствовать различные точки зрения
- ✅ Принимать конструктивную критику
- ✅ Фокусироваться на том, что лучше для сообщества
- ✅ Проявлять эмпатию к другим участникам

### Недопустимое поведение

- ❌ Использование сексуализированного языка или изображений
- ❌ Троллинг, оскорбительные комментарии
- ❌ Личные или политические атаки
- ❌ Публичные или приватные домогательства
- ❌ Публикация личной информации других без разрешения

---

## Как внести вклад

### Сообщить об ошибке

Если вы нашли ошибку, пожалуйста:

1. **Проверьте**, что ошибка еще не зарегистрирована в Issues
2. **Создайте новый Issue** с:
   - Описательным заголовком
   - Подробным описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемым поведением
   - Фактическим поведением
   - Скриншотами (если возможно)
   - Версией браузера/ОС
   - Логами (если есть)

**Шаблон Issue для ошибки:**

```markdown
## Описание
Краткое описание проблемы.

## Шаги для воспроизведения
1. Перейти на '...'
2. Нажать на '...'
3. Прокрутить до '...'
4. Увидеть ошибку

## Ожидаемое поведение
Что должно было произойти.

## Фактическое поведение
Что произошло на самом деле.

## Окружение
- ОС: [например, Windows 10]
- Браузер: [например, Chrome 120]
- Node.js: [например, 18.17.0]
- PostgreSQL: [например, 14.5]

## Дополнительный контекст
Логи, скриншоты, и т.д.
```

### Предложить улучшение

Есть идея для новой функции?

1. **Проверьте**, что предложение еще не существует
2. **Создайте Issue** с тегом "enhancement"
3. **Опишите**:
   - Проблему, которую решает функция
   - Предлагаемое решение
   - Альтернативы, которые вы рассматривали
   - Примеры использования

**Шаблон Issue для улучшения:**

```markdown
## Проблема
Описание проблемы или потребности.

## Предлагаемое решение
Подробное описание предлагаемой функции.

## Альтернативы
Другие решения, которые вы рассматривали.

## Примеры использования
Как это будет использоваться на практике.

## Дополнительная информация
Mockups, диаграммы, ссылки, и т.д.
```

---

## Процесс разработки

### 1. Fork репозиторий

Нажмите кнопку "Fork" на странице репозитория.

### 2. Клонируйте ваш fork

```bash
git clone https://github.com/YOUR-USERNAME/attendance-monitor.git
cd attendance-monitor
```

### 3. Добавьте upstream remote

```bash
git remote add upstream https://github.com/ORIGINAL-OWNER/attendance-monitor.git
```

### 4. Создайте ветку для вашей работы

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
```

**Соглашение об именовании веток:**

- `feature/` - новая функциональность
- `fix/` - исправление ошибок
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `test/` - добавление/изменение тестов
- `chore/` - обслуживание (зависимости, конфигурация)

### 5. Установите зависимости

```bash
npm install
```

### 6. Настройте окружение

```bash
# Создайте .env файл
cp .env.example .env

# Настройте БД
npm run prisma:migrate
npm run prisma:seed
```

### 7. Внесите изменения

Следуйте [стандартам кода](#стандарты-кода).

### 8. Запустите тесты

```bash
# Когда тесты будут добавлены
npm test

# Проверьте линтинг
npm run lint

# Проверьте сборку
npm run build
```

### 9. Commit изменения

Следуйте [соглашению о commit сообщениях](#commit-сообщения).

```bash
git add .
git commit -m "feat: add new feature"
```

### 10. Синхронизируйтесь с upstream

```bash
git fetch upstream
git rebase upstream/main
```

### 11. Push в ваш fork

```bash
git push origin feature/your-feature-name
```

### 12. Создайте Pull Request

Откройте PR на GitHub с описанием изменений.

---

## Стандарты кода

### JavaScript/React

**Общие правила:**

- ✅ Используйте ES6+ синтаксис
- ✅ Используйте функциональные компоненты с хуками
- ✅ Избегайте глобальных переменных
- ✅ Используйте `const` по умолчанию, `let` когда нужно
- ✅ Используйте стрелочные функции где возможно
- ✅ Добавляйте комментарии только когда необходимо объяснить "почему"

**Именование:**

- **Компоненты**: PascalCase (`UserCard`, `LoginPage`)
- **Функции/переменные**: camelCase (`getUserData`, `isLoading`)
- **Константы**: UPPER_SNAKE_CASE (`API_BASE_URL`, `MAX_RETRIES`)
- **Файлы компонентов**: PascalCase (`UserCard.jsx`)
- **Файлы утилит**: camelCase (`apiClient.js`, `validation.js`)

**Структура компонента:**

```javascript
// 1. Imports
import React, { useState, useEffect } from 'react';
import axios from 'axios';

// 2. Constants (если нужны)
const MAX_ATTEMPTS = 3;

// 3. Component
function UserCard({ user }) {
  // 3.1. Hooks
  const [loading, setLoading] = useState(false);
  const [data, setData] = useState(null);

  // 3.2. useEffect
  useEffect(() => {
    fetchData();
  }, []);

  // 3.3. Functions
  const fetchData = async () => {
    // ...
  };

  // 3.4. Render conditions
  if (loading) return <div>Loading...</div>;

  // 3.5. Main render
  return (
    <div className="user-card">
      <h2>{user.name}</h2>
    </div>
  );
}

// 4. Export
export default UserCard;
```

### CSS

- ✅ Используйте kebab-case для классов (`user-card`, `btn-primary`)
- ✅ Избегайте !important
- ✅ Используйте BEM методологию где возможно
- ✅ Организуйте стили по компонентам

### Backend (Node.js/Express)

**Структура роута:**

```javascript
// routes/example.js
const express = require('express');
const router = express.Router();
const authMiddleware = require('../middleware/authMiddleware');
const exampleController = require('../controllers/exampleController');

// GET /api/example
router.get('/', authMiddleware, exampleController.getAll);

// POST /api/example
router.post('/', authMiddleware, exampleController.create);

module.exports = router;
```

**Структура контроллера:**

```javascript
// controllers/exampleController.js
const prisma = require('../prismaClient');

exports.getAll = async (req, res) => {
  try {
    const items = await prisma.example.findMany();
    res.json({ items });
  } catch (error) {
    console.error('Error:', error);
    res.status(500).json({ error: 'Internal server error' });
  }
};
```

### База данных (Prisma)

- ✅ Используйте camelCase для полей модели
- ✅ Используйте PascalCase для имен моделей
- ✅ Создавайте индексы для часто используемых полей
- ✅ Документируйте сложные связи

```prisma
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  fullName  String?
  createdAt DateTime @default(now())
  
  // Relations
  posts     Post[]
  
  @@index([email])
}
```

---

## Commit сообщения

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

### Формат

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы

- `feat` - новая функция
- `fix` - исправление ошибки
- `docs` - изменения в документации
- `style` - форматирование, пробелы (не CSS)
- `refactor` - рефакторинг кода
- `test` - добавление/изменение тестов
- `chore` - обслуживание, зависимости
- `perf` - улучшение производительности

### Примеры

```bash
# Новая функция
feat(auth): add two-factor authentication

# Исправление ошибки
fix(camera): resolve permission issue on iOS

# Документация
docs(readme): update installation instructions

# Рефакторинг
refactor(api): simplify user validation logic

# Множественные файлы
feat(ui): redesign login page and add animations
- Redesigned login form with new layout
- Added fade-in animations
- Improved mobile responsiveness
```

---

## Pull Request процесс

### Перед созданием PR

- ✅ Убедитесь, что код работает локально
- ✅ Проверьте, что все тесты проходят
- ✅ Обновите документацию при необходимости
- ✅ Синхронизируйтесь с main веткой
- ✅ Проверьте, что нет конфликтов

### Описание PR

**Шаблон:**

```markdown
## Описание
Краткое описание изменений.

## Тип изменения
- [ ] Bug fix (исправление)
- [ ] New feature (новая функция)
- [ ] Breaking change (ломающее изменение)
- [ ] Documentation update (обновление документации)

## Как протестировано
Опишите как вы тестировали изменения.

## Чеклист
- [ ] Код следует стандартам проекта
- [ ] Self-review выполнен
- [ ] Код прокомментирован в сложных местах
- [ ] Документация обновлена
- [ ] Изменения не генерируют новые warnings
- [ ] Тесты добавлены/обновлены
- [ ] Все тесты проходят
- [ ] Зависимые изменения объединены

## Связанные Issues
Closes #123
Relates to #456

## Скриншоты (если применимо)
Добавьте скриншоты для визуальных изменений.
```

### Code Review

Будьте готовы к:

- Обсуждению архитектурных решений
- Запросам на изменения
- Вопросам о реализации
- Предложениям по улучшению

**Как реагировать на feedback:**

- ✅ Будьте открыты к предложениям
- ✅ Задавайте вопросы если что-то неясно
- ✅ Вносите запрошенные изменения
- ✅ Отмечайте разрешенные комментарии

### После одобрения

После одобрения maintainer:

1. Выполнит squash and merge
2. Удалит ветку
3. Закроет связанные Issues

---

## Типы вкладов

### 🐛 Исправление ошибок

Нашли баг? Отлично!

1. Создайте Issue с описанием
2. Создайте PR с исправлением
3. Добавьте тест, воспроизводящий баг

### ✨ Новая функциональность

Хотите добавить новую функцию?

1. Сначала создайте Issue для обсуждения
2. Дождитесь одобрения
3. Создайте PR с реализацией
4. Добавьте документацию
5. Добавьте тесты

### 📝 Документация

Улучшения документации всегда приветствуются:

- Исправление опечаток
- Улучшение объяснений
- Добавление примеров
- Перевод на другие языки

### 🧪 Тесты

Помогите повысить покрытие тестами:

- Юнит тесты
- Интеграционные тесты
- E2E тесты

### 🎨 UI/UX улучшения

Дизайнер? Мы рады!

- Улучшение существующего UI
- Новые компоненты
- Анимации
- Адаптивность

### 🌍 Локализация

Помогите сделать приложение доступным на других языках.

---

## Вопросы?

Если у вас есть вопросы:

1. Проверьте [FAQ.md](./FAQ.md)
2. Проверьте существующие Issues
3. Создайте новый Issue с вопросом
4. Свяжитесь с maintainers

---

## Благодарности

Спасибо за ваш вклад! 🎉

Каждый PR, Issue, или предложение помогает сделать проект лучше.

**Особая благодарность всем контрибьюторам!** 🙏

---

**Счастливого кодинга!** 💻
