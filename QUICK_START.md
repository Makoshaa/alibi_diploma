# Быстрый старт - Работа в локальной сети

## 🚀 Запуск приложения

Просто выполните команду:

```bash
npm run dev
```

Скрипт **автоматически**:
- ✅ Определит ваш локальный IP адрес (исключая виртуальные адаптеры)
- ✅ Создаст `.env.local` с правильными настройками
- ✅ Запустит сервер и клиент

## 📱 Подключение с мобильного устройства

После запуска `npm run dev` в консоли появится сообщение:

```
✅ HTTPS Server listening on port 5000
Local:   https://localhost:5000
Network: https://172.20.10.2:5000

📱 Для подключения мобильных устройств:
   1. Убедитесь, что устройство подключено к той же Wi-Fi сети
   2. Откройте https://172.20.10.2:5173 в браузере мобильного устройства
   3. Примите предупреждение о самоподписанном сертификате
```

### Важно:
1. **Используйте IP адрес из консоли** (в вашем случае это `172.20.10.2`)
2. **Подключите мобильное устройство к той же Wi-Fi сети**
3. **Примите предупреждение о сертификате** на мобильном устройстве

## 🔧 Если IP адрес изменился

Если вы переключились на другую Wi-Fi сеть или IP изменился:

```bash
npm run setup:network
```

Эта команда обновит `.env.local` с новым IP адресом.

## ❓ Проблемы и решения

### "Не могу открыть на мобильном"
- ✅ Убедитесь, что оба устройства в одной Wi-Fi сети
- ✅ Используйте IP адрес из консоли (не localhost!)
- ✅ Примите предупреждение о сертификате в браузере

### "Висит загрузка..."
- ✅ Проверьте, что API работает: откройте `https://172.20.10.2:5000/api/health`
- ✅ Перезапустите приложение: `npm run dev`
- ✅ Очистите кэш браузера на мобильном устройстве

### "Неправильный IP адрес"
- ✅ Запустите: `npm run setup:network`
- ✅ Проверьте свой IP: `ipconfig` (Windows) или `ifconfig` (Mac/Linux)

## 🔒 О сертификатах

Приложение использует HTTPS с самоподписанным сертификатом для безопасности.

**Как принять предупреждение:**

**iPhone/iPad (Safari):**
- Нажмите "Показать подробности"
- Нажмите "Посетить этот веб-сайт"
- Введите пароль/Touch ID если потребуется

**Android (Chrome):**
- Нажмите "Дополнительно"
- Нажмите "Перейти на сайт (небезопасно)"

## 📋 Что изменилось

### Автоматическое определение IP
- ✅ Исключает виртуальные адаптеры (WSL, Hyper-V, VirtualBox)
- ✅ Приоритизирует Wi-Fi и реальные сетевые адаптеры
- ✅ Поддерживает все диапазоны частных IP (192.168.x.x, 10.x.x.x, 172.16-31.x.x)

### Правильная загрузка ресурсов
- ✅ API запросы используют правильный IP адрес
- ✅ Face-API модели загружаются с правильного URL
- ✅ Liveness WASM модули загружаются с правильного URL
- ✅ CORS настроен для работы в локальной сети

## 📝 Файлы конфигурации

### `.env` (основная конфигурация)
Редактируется вручную для настройки БД, JWT и т.д.

### `.env.local` (автоматически генерируется)
**НЕ РЕДАКТИРУЙТЕ ВРУЧНУЮ!** Этот файл автоматически создается при каждом запуске `npm run dev`.
